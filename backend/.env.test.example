# Dedicated PostgreSQL test database. No fallback to DATABASE_URL.
TEST_DATABASE_URL=
# Enable only when no development DATABASE_URL is configured and this DB is exclusively for tests.
TEST_DATABASE_ONLY=false
