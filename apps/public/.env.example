# Server-only API URL; no database credentials belong in this workspace.
API_BASE_URL=http://127.0.0.1:4100/api/v1

# Canonical public origin used to build product links/SEO, e.g. https://catalogo.example.com
NEXT_PUBLIC_SITE_URL=http://localhost:3000
# Server-only shared secret; use the same value as backend REVALIDATION_SECRET.
REVALIDATION_SECRET=
