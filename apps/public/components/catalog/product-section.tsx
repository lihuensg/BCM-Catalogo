import Link from'next/link';import type{PublicProductListDto}from'@bcm/shared';import{ProductCard}from'./product-card';
export function ProductSection({eyebrow,title,description,href,products,alt=false}:{eyebrow:string;title:string;description?:string;href:string;products:PublicProductListDto[];alt?:boolean}){if(!products.length)return null;return <section className={'public-section'+(alt?' alt':'')}><div className="container"><div className="section-head"><div><div className="eyebrow">{eyebrow}</div><h2>{title}</h2>{description&&<p>{description}</p>}</div><Link className="section-link" href={href}>Ver todos →</Link></div><div className="product-grid">{products.map(p=><ProductCard key={p.id} product={p}/>)}</div></div></section>}
