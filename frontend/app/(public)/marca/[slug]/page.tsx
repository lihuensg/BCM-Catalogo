import{notFound}from'next/navigation';import{CatalogListing}from'@/components/catalog/catalog-listing';import{getBrands}from'@/services/public/client';export default async function Page({params,searchParams}:{params:Promise<{slug:string}>;searchParams:Promise<Record<string,string|string[]|undefined>>}){const{slug}=await params,b=(await getBrands()).find(x=>x.slug===slug);if(!b)notFound();return <CatalogListing pathname={'/marca/'+slug} params={await searchParams} forced={{brand:slug}} title={b.name} description={b.description??'Explorá los productos publicados de esta marca.'}/>}
