import{notFound}from'next/navigation';import{CatalogListing}from'@/components/catalog/catalog-listing';import{getCategories}from'@/services/public/client';export default async function Page({params,searchParams}:{params:Promise<{slug:string}>;searchParams:Promise<Record<string,string|string[]|undefined>>}){const{slug}=await params,c=(await getCategories()).find(x=>x.slug===slug);if(!c)notFound();return <CatalogListing pathname={'/categoria/'+slug} params={await searchParams} forced={{category:slug}} title={c.name} description={c.description??'Explorá los productos disponibles en esta categoría.'}/>}
