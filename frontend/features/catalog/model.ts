import type {Availability,PublicProductListDto,PublicProductListQuery,PublicSettingsDto} from '@bcm/shared';
export const availabilityLabel:Record<Availability,string>={AVAILABLE:'Disponible',LOW_STOCK:'Pocas unidades',OUT_OF_STOCK:'Sin stock',CHECK_AVAILABILITY:'Consultar disponibilidad',MADE_TO_ORDER:'Por encargo'};
export function formatAmount(value:string|null){if(value===null)return null;const [whole,fraction='00']=value.split('.');return whole!.replace(/\B(?=(\d{3})+(?!\d))/g,'.')+','+fraction.padEnd(2,'0');}
export function publicPrice(product:Pick<PublicProductListDto,'showPrice'|'price'>){return product.showPrice&&product.price?formatAmount(product.price):null;}
export function whatsappUrl(settings:PublicSettingsDto|null,product?:Pick<PublicProductListDto,'name'|'slug'|'price'|'showPrice'>&{sku?:string|null},origin=''){if(!settings?.whatsappNumber)return null;const number=settings.whatsappNumber.replace(/\D/g,'');if(!number)return null;let t=settings.whatsappMessageTemplate??'';const values:{[key:string]:string}={productName:product?.name??'',productUrl:product?origin.replace(/\/$/,'')+'/producto/'+product.slug:'',sku:product?.sku??'',price:product?.showPrice&&product.price?formatAmount(product.price)??'':''};for(const [k,v] of Object.entries(values))t=t.replaceAll('{{'+k+'}}',v);t=t.replace(/\{\{[^}]+\}\}/g,'').replace(/\s{2,}/g,' ').trim();return 'https://wa.me/'+number+(t?'?text='+encodeURIComponent(t):'');}
export function catalogQuery(params:Record<string,string|string[]|undefined>,forced:Partial<PublicProductListQuery>={}):PublicProductListQuery{const first=(k:string)=>{const v=params[k];return Array.isArray(v)?v[0]:v};const p=Number(first('page')??'1');const sorts=['sortOrder','publishedAt','price','name'] as const;const raw=first('sort');return{page:Number.isInteger(p)&&p>0?p:1,pageSize:24,search:first('search')?.trim()||undefined,category:first('category')||undefined,brand:first('brand')||undefined,sort:sorts.includes(raw as typeof sorts[number])?raw as typeof sorts[number]:'sortOrder',order:first('order')==='desc'?'desc':'asc',...forced};}
